import { assetUrl, formatDate } from "../../utils/helpers";
export default function GalleryCard({ item, onDelete }) { const image=assetUrl(item.fileUrl); return <article className="gallery-card">{item.fileType==="video"?<video src={image} controls/>:<img src={image} alt={item.caption||item.event?.title||"Event gallery"}/>}<div className="gallery-caption"><div><strong>{item.event?.title||"Event"}</strong><p>{item.caption||"EventSphere memory"}</p><small>{formatDate(item.createdAt)}</small></div>{onDelete&&<button className="btn btn-danger btn-sm" onClick={()=>onDelete(item._id)}>Delete</button>}</div></article>; }
