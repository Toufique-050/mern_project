import { Link } from "react-router-dom"; import EventStatus from "./EventStatus"; import { assetUrl, formatDate } from "../../utils/helpers";
export default function EventCard({ event, action }) { return <article className="event-card"><div className="event-cover">{event.bannerImage?<img src={assetUrl(event.bannerImage)} alt=""/>:<div className="cover-placeholder"><span>{event.category?.slice(0,1)||"E"}</span></div>}<div className="cover-date"><strong>{new Date(event.date).getDate()}</strong><small>{new Date(event.date).toLocaleDateString("en-US",{month:"short"})}</small></div></div><div className="event-body"><div className="event-top"><span className="chip">{event.category}</span><EventStatus status={event.status}/></div><h3>{event.title}</h3><p>{event.description}</p><div className="event-meta"><span>📍 {event.venue}</span><span>🕒 {event.time}</span></div><div className="event-foot"><span>{event.availableSeats ?? event.maxParticipants} seats available</span>{action || <Link className="btn btn-primary btn-sm" to={`/events/${event._id}`}>View details</Link>}</div></div></article>; }
